Steps to deploy to GCP Cloud Run:
docker build -t
gcloud artifacts repositories create agent-mailman --repository-format=docker --location=us-east4 --description="Docker repository for hello world
gcloud auth configure-docker us-east4-docker.pkg.dev
docker tag agent-mailman us-east4-docker.pkg.dev/pytutoring-dev/agent-mailman/agent-mailman
docker push us-east4-docker.pkg.dev/pytutoring-dev/agent-mailman/agent-mailman
gcloud run deploy hello-world-service --image YOUR_REGION-docker.pkg.dev/YOUR_PROJECT_ID/YOUR_REPO_NAME/hello-world:latest --platform managed --region YOUR_REGION


To-Do:
- when language for text-to-speech is receiving too much data (languages like Mandarin and Russian), add proper error reporting and create a graceful dismount
- do the same in index.html
- fix Hindi output